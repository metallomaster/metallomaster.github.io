pre-commit:
  parallel: true
  commands:
    prettier:
      glob: '*.{ts,mjs,astro,css,json,md,yml}'
      run: npx prettier --check {staged_files}
    eslint:
      glob: '*.{ts,mjs,astro}'
      run: npx eslint {staged_files}

pre-push:
  commands:
    typecheck:
      run: npm run check
