---
import { ThemeToggle } from '@features/theme-toggle';
import { siteConfig } from '@shared/config';
import { Container, Logo } from '@shared/ui';

const navItems = [
  { label: 'Главная', href: '/' },
  { label: 'Каталог', href: '/catalog/' },
  { label: 'Монтаж', href: '/services/chimney-installation/' },
  { label: 'Контакты', href: '/contacts/' },
];

// Нормализуем путь к виду с завершающим слешем (trailingSlash: 'always')
const currentPath = Astro.url.pathname.endsWith('/')
  ? Astro.url.pathname
  : `${Astro.url.pathname}/`;
const isCurrent = (href: string) =>
  href === '/' ? currentPath === '/' : currentPath.startsWith(href);
---

<header class="header" data-menu-open="false">
  <Container class="header__inner">
    <a class="header__logo" href="/" aria-label="METALLOMASTER — на главную">
      <Logo idPrefix="header-logo" />
    </a>

    <nav class="header__nav" aria-label="Основная навигация" data-menu-panel>
      <ul class="header__nav-list">
        {
          navItems.map((item) => (
            <li>
              <a
                class="header__nav-link"
                href={item.href}
                aria-current={isCurrent(item.href) ? 'page' : undefined}
              >
                {item.label}
              </a>
            </li>
          ))
        }
      </ul>
      <a class="header__phone header__phone--mobile" href={`tel:${siteConfig.phone}`}>
        {siteConfig.phoneDisplay}
      </a>
      <div class="header__theme-row">
        <span class="header__theme-label">Тема</span>
        <ThemeToggle />
      </div>
    </nav>

    <div class="header__actions">
      <a class="header__phone" href={`tel:${siteConfig.phone}`}>{siteConfig.phoneDisplay}</a>
      <span class="header__theme-desktop"><ThemeToggle /></span>
      <button
        class="header__burger"
        type="button"
        data-menu-toggle
        aria-expanded="false"
        aria-label="Открыть меню"
      >
        <span class="header__burger-line"></span>
        <span class="header__burger-line"></span>
        <span class="header__burger-line"></span>
      </button>
    </div>
  </Container>
</header>

<style>
  .header {
    position: sticky;
    top: 0;
    z-index: 100;
    background: var(--color-bg);
    border-bottom: 1px solid var(--color-border);
  }

  .header :global(.header__inner) {
    display: flex;
    align-items: center;
    gap: var(--space-5);
    min-height: var(--header-height);
  }

  .header__logo {
    text-decoration: none;
    flex-shrink: 0;
  }

  .header__nav {
    margin-inline-start: auto;
  }

  .header__nav-list {
    display: flex;
    gap: var(--space-2);
    list-style: none;
    padding: 0;
  }

  .header__nav-link {
    display: inline-flex;
    align-items: center;
    min-height: 44px;
    padding: var(--space-1) var(--space-3);
    border-radius: var(--radius-sm);
    font-weight: 600;
    font-size: var(--text-sm);
    text-decoration: none;
    color: var(--color-text-muted);
    transition: color var(--transition-fast);
  }

  .header__nav-link:hover {
    color: var(--color-text);
  }

  .header__nav-link[aria-current='page'] {
    color: var(--color-accent);
  }

  .header__actions {
    display: flex;
    align-items: center;
    gap: var(--space-3);
  }

  .header__phone {
    font-weight: 800;
    font-size: var(--text-sm);
    text-decoration: none;
    white-space: nowrap;
  }

  .header__phone:hover {
    color: var(--color-accent);
  }

  .header__phone--mobile {
    display: none;
  }

  /* Строка «Тема» с переключателем — только в мобильной панели меню */
  .header__theme-row {
    display: none;
  }

  .header__theme-desktop {
    display: inline-flex;
  }

  .header__burger {
    display: none;
    flex-direction: column;
    justify-content: center;
    gap: 5px;
    width: 44px;
    height: 44px;
    padding: 10px;
    border: none;
    border-radius: var(--radius-md);
    background: transparent;
    cursor: pointer;
  }

  .header__burger-line {
    height: 2px;
    width: 100%;
    background: var(--color-text);
    border-radius: 1px;
    transition:
      transform var(--transition-base),
      opacity var(--transition-base);
  }

  @media (max-width: 1023px) {
    .header__phone:not(.header__phone--mobile) {
      display: none;
    }

    /* На узких экранах в строке только логотип и бургер справа */
    .header__theme-desktop {
      display: none;
    }

    .header__actions {
      margin-inline-start: auto;
    }

    .header__burger {
      display: flex;
    }

    .header__nav {
      position: absolute;
      inset-inline: 0;
      top: 100%;
      display: none;
      background: var(--color-bg);
      border-bottom: 1px solid var(--color-border);
      box-shadow: var(--shadow-md);
      padding: var(--space-4) var(--container-pad) var(--space-5);
    }

    .header[data-menu-open='true'] .header__nav {
      display: block;
    }

    .header[data-menu-open='true'] .header__burger-line:nth-child(1) {
      transform: translateY(7px) rotate(45deg);
    }

    .header[data-menu-open='true'] .header__burger-line:nth-child(2) {
      opacity: 0;
    }

    .header[data-menu-open='true'] .header__burger-line:nth-child(3) {
      transform: translateY(-7px) rotate(-45deg);
    }

    .header__nav-list {
      flex-direction: column;
      gap: 0;
    }

    .header__nav-link {
      width: 100%;
      min-height: 48px;
      font-size: var(--text-base);
    }

    .header__phone--mobile {
      display: inline-flex;
      align-items: center;
      min-height: 48px;
      padding: var(--space-1) var(--space-3);
      color: var(--color-accent);
    }

    .header__theme-row {
      display: flex;
      align-items: center;
      justify-content: space-between;
      min-height: 48px;
      margin-block-start: var(--space-2);
      padding-inline: var(--space-3);
      border-top: 1px solid var(--color-border);
    }

    .header__theme-label {
      font-weight: 600;
      font-size: var(--text-base);
      color: var(--color-text-muted);
    }
  }
</style>

<script>
  import { initMobileMenu } from '../model/menu';

  document.querySelectorAll<HTMLElement>('.header').forEach(initMobileMenu);
</script>
