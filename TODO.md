# TODO

Сайт выложен на metallomaster.by 14.09.2026 вручную через ispmanager. Проверено:
`.htaccess` читается, все 89 старых адресов отдают 301 одним переходом, http → https,
www → без www, 404 работает, форма боевая.

## Автодеплой — что осталось сделать

Workflow уже в репозитории: `.github/workflows/production.yml` (ветка `main` → domain.by)
и `preview.yml` (ветка `dev` → GitHub Pages). Боевой прогон пропускается, пока не задана
переменная `DEPLOY_ENABLED`.

### 1. SSH на хостинге

- [ ] Проверить, включён ли он уже: `ssh metallomaster@93.84.119.243` со своего компьютера.
      Пароль тот же, что от ispmanager. Порт 22 снаружи отвечает отказом, так что он
      либо выключен, либо нестандартный.
- [ ] Если выключен — письмо на `support@domain.by` с контактного адреса **хостингового**
      аккаунта: просьба включить SSH и сообщить хост, порт и имя пользователя.
      **Порт спросить обязательно** — он нужен точным для секрета `SSH_PORT`.

### 2. Ключ деплоя

- [ ] Создать отдельную пару, не использовать личный ключ:

      ssh-keygen -t ed25519 -f ~/.ssh/metallomaster_deploy -C "github-actions" -N ""

- [ ] Публичную часть (`metallomaster_deploy.pub`) дописать в `~/.ssh/authorized_keys`
      на хостинге. Через файловый менеджер ispmanager: домашняя директория → `.ssh` →
      `authorized_keys` (нет файла — создать), права `600`.
- [ ] Проверить вход по ключу:

      ssh -i ~/.ssh/metallomaster_deploy -p ПОРТ metallomaster@93.84.119.243

### 3. Секреты в GitHub

Settings → Secrets and variables → Actions → вкладка **Secrets**:

- [ ] `SSH_PRIVATE_KEY` — содержимое `~/.ssh/metallomaster_deploy` целиком,
      вместе со строками `-----BEGIN` и `-----END`
- [ ] `SSH_HOST` — `93.84.119.243`
- [ ] `SSH_PORT` — порт из ответа поддержки
- [ ] `SSH_USER` — `metallomaster`
- [ ] `WEB3FORMS_KEY` — ключ формы, иначе соберётся демо-режим

### 4. Включить деплой

- [ ] Вкладка **Variables** → `DEPLOY_ENABLED` = `true`
- [ ] Settings → Pages → Source → **GitHub Actions** (для превью)
- [ ] Settings → Environments → `github-pages` → Deployment branches → добавить `dev`,
      иначе превью с этой ветки отказывается деплоиться
- [ ] По желанию: Settings → Environments → `production` → Required reviewers —
      тогда Actions соберёт сайт и остановится, ожидая подтверждения выката

### 5. Проверить

- [ ] Push в `dev` → превью собралось на GitHub Pages
- [ ] Merge в `main` → боевой сайт обновился, прогон зелёный
- [ ] Убедиться, что `.well-known` и `cgi-bin` на сервере на месте после первой
      автовыгрузки: `rsync --delete` их исключает, но проверить стоит один раз

## Хвосты после релиза

- [ ] **Кэш для `/_astro/` и `/fonts/`.** Статика отдаётся вообще без `Cache-Control` —
      каждый повторный визит перекачивает шрифты, стили и картинки заново. nginx отдаёт её
      сам, мимо Apache, поэтому раздел 5 `.htaccess` не применяется. Проверено 15.09.2026
      через `curl -sI https://metallomaster.by/_astro/base-layout.BkR_ssfe.css`: до этих
      файлов не доходит НИ ОДИН заголовок из `.htaccess` — ни `Cache-Control`, ни
      `X-Content-Type-Options`, ни CSP, — тогда как на `/` приходят все. Lighthouse считает
      потерю в 43 КБ на повторный визит, из них 38 КБ — два шрифта.
      Письмо в `support@domain.by` — попросить для домена задать
      `Cache-Control: public, max-age=31536000, immutable` на `location ^~ /_astro/`
      и на `location ^~ /fonts/`.
      В `/_astro/` имена хэшированные, вечный кэш безопасен по определению. В `/fonts/`
      имена постоянные — если шрифт когда-нибудь менять, класть его под новым именем,
      иначе у вернувшихся посетителей останется старый. Альтернатива на будущее: перенести
      woff2 из `public/fonts/` в `src/`, тогда Astro сам добавит к ним хэш и положит
      в `/_astro/` — останется одно правило вместо двух (правку в `config/fonts.css`
      и в preload внутри `base-layout.astro` делать одновременно).
      Если поддержка откажется править конфиг — в ispmanager у домена есть переключатель
      обработки статики nginx. Выключить его: всё пойдёт через Apache, и `.htaccess`
      заработает целиком, включая заголовки безопасности на статике. Отдача чуть медленнее.
- [ ] **Ограничить ключ Web3Forms доменом** `metallomaster.by` в кабинете сервиса.
      Ключ виден в исходнике страницы — это единственная настоящая защита от чужого спама.
- [ ] **Отправить тестовую заявку** с живого сайта: проверить, что письмо дошло
      на `3220010@mail.ru` и не улетело в спам.
- [ ] **Search Console и Яндекс.Вебмастер**: отправить новую `sitemap.xml` (38 адресов),
      следить за переиндексацией. Сайт в обоих сервисах уже подтверждён — файлы
      подтверждения лежат в `public/`, удалять их нельзя.
- [ ] **Регион «Минск»** в Яндекс.Вебмастере: Представление в поиске → Региональность.
- [ ] **Через неделю спокойной работы** — поднять HSTS с `max-age=300` до
      `max-age=63072000; includeSubDomains` (`public/.htaccess`, раздел 4). Сразу два года
      ставить нельзя: браузеры запомнят требование https на весь срок.
- [ ] **CAA-запись в DNS.** Просто, полчаса.

## Карточки на картах

- [ ] **Яндекс.Бизнес**: карточка существует — `yandex.by/maps/org/metallomaster/212145119949`.
      Метка стоит верно (боксы ГСПК «Колодищи», 53.952978, 27.789054), но подпись адреса
      осталась старая — «Путейская улица», и расходится с сайтом. Забрать права, исправить.
- [ ] **Google Business Profile**: карточки нет совсем, поиск по телефону не находит.
      Создать, добавить фото работ.

## Контент и мелочи

- [ ] **Фото водостоков.** Категория собрана из того, что нашлось на старом сайте:
      обложка всего 187×187, остальные немногим больше. Нужны нормальные снимки
      готовых водостоков своего производства.
- [ ] **`src/pages/404.astro`** — пропала кнопка «Каталог продукции», а текст на странице
      по-прежнему зовёт заглянуть в каталог. Решить: вернуть кнопку или поправить текст.

## Можно добавить позже — решено сейчас не делать

- [ ] **Аналитика.** На старом сайте стояли GTM `GTM-K9QW5T4` и Universal Analytics
      `UA-21103506-1` (последний мёртв сам по себе — Google отключил UA в июле 2023).
      На новом счётчиков нет, поэтому нет и cookie-баннера: сайт ничего не пишет ни в
      cookie, ни в localStorage. Подключите аналитику — понадобятся баннер, цели
      (клик по телефону, отправка формы) и **правка раздела 2 политики конфиденциальности**,
      где сейчас написано «Сайт не использует счётчики».
      Отдельно: Яндекс.Метрика не умеет в prerender — ей нужна проверка
      `document.prerendering`, иначе в отчётах появятся просмотры, которых не было
      (см. комментарий в `src/lib/speculation.ts`).
- [ ] **Контекстная реклама**: Google Ads / Яндекс.Директ. Посадочные страницы уже есть —
      категории каталога подходят как есть.
- [ ] **Соцсети.** На старом сайте в шапке висел личный Instagram `instagram.com/vasieleek`,
      на новом соцсетей нет — решение владельца от 19.08.2026. Появится брендовый аккаунт —
      добавить ссылку в футер и `sameAs` в разметку организации.
- [ ] **FAQ-блоки на категориях**, справочный раздел под нормативные запросы,
      квиз-калькулятор.

## Подтверждено владельцем

- 19.08.2026 — почта для заявок `3220010@mail.ru`, регистрация ресурса в БелГИЭ
  у domain.by действует, почтовый индекс производства 223050, крошки на страницах
  не показываем, цены не публикуем.
- 14.09.2026 — режим работы «Пн–Пт 9:00–17:00», категория водосточных систем нужна:
  изделия делают, спрос в поиске есть.
